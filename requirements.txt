# Core dependencies
plotly>=5.18
pandas>=2.0
numpy>=1.24
scipy>=1.11
statsmodels>=0.14
scikit-learn>=1.3
requests>=2.31
python-dotenv>=1.0
oura-ring>=0.4

# Causal impact analysis (required by analyze_oura_causal.py)
pycausalimpact>=0.2

# Entropy / nonlinear HRV metrics (required by advanced_hrv + biomarkers)
antropy>=0.1
nolds>=0.5

# Optional: Foundation model forecasting (requires GPU)
# chronos-forecasting>=1.0
# torch>=2.0

# Optional: Causal discovery (PCMCI+)
# tigramite>=5.2

# Optional: Matrix Profile anomaly detection
# stumpy>=1.12

# Optional: Time-series feature extraction
# tsfresh>=0.20

# Optional: Kalman filter digital twin
filterpy>=1.4
# pykalman>=0.9

# Optional: HMM state detection (GVHD prediction)
hmmlearn>=0.3
