# ---------------------------------------------------------------------------
# Core dependencies - enough to run the basic pipeline.
#
#   pip install -r requirements.txt
#
# For the full stack (all optional backends), install requirements-full.txt
# instead:
#
#   pip install -r requirements-full.txt
# ---------------------------------------------------------------------------

numpy>=1.24

# Core dependencies
plotly>=5.18
pandas>=2.0
scipy>=1.11
statsmodels>=0.14
scikit-learn>=1.3
requests>=2.31
python-dotenv>=1.0
# Latest published version is 0.3.0 at time of writing.
oura-ring>=0.3,<0.4

pykalman>=0.9

# Data validation (required by data_schemas.py)
pandera>=0.18
